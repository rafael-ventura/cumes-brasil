# JSONs — Backup / Referência

Estes arquivos são mantidos apenas como **backup e referência** para recuperação futura.

**Fonte atual dos dados:** `src/Infrastructure/data/*.yaml`

O seed (`npm run seed`) carrega os YAMLs no banco.
