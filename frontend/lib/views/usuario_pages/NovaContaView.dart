import 'package:flutter/material.dart';

class NovaContaView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        width: 100,
        height: 100,
        color: Colors.amber,
        child: Text('Perfil'),
      ),
    );
  }
}
