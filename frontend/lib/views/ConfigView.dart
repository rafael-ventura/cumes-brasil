import 'package:flutter/material.dart';

class ConfigView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('PageConfig')],
    );
  }
}
