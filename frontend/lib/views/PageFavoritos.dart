import 'package:flutter/material.dart';

class PageFavoritos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text('PageFavoritos')],
    );
  }
}
